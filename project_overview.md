# نظرة عامة وشاملة لمشروع LearnNeur (Learnova App)

هذا الملف عبارة عن توثيق ملخص وشامل لجميع تفاصيل وملفات المشروع بناءً على قراءة دقيقة لهيكل الملفات، إعدادات الحزم، الكود البرمجي الأساسي، ومحتويات المشروع.

## 🌟 نبذة عن المشروع
**LearnNeur** هو تطبيق ويب تفاعلي ومخصص لدعم أطفال طيف التوحد، وأسرهم، والأطباء المتخصصين.
يعمل التطبيق بنظام **Local-First** بالكامل باستخدام `localStorage` بدلاً من قواعد البيانات الخارجية لضمان السرعة العالية وحماية الخصوصية. كما يوفر بيئة متعددة الأدوار (الطفل، ولي الأمر، الطبيب) بخصائص تناسب كل مستخدم.

---

## 🛠️ التقنيات والمكتبات المستخدمة
تم بناء المشروع باستخدام أحدث تقنيات تطوير واجهات الويب (Frontend):
- **React (v19.2.0)**: الواجهة الأساسية للتطبيق.
- **Vite (v7.3.1)**: بيئة التطوير وأداة البناء السريعة.
- **TailwindCSS (v4.2.1)**: مكتبة التنسيق (CSS) لإدارة تصميم التطبيق.
- **HeroUI (v2.8.10)**: مجموعة مكونات جاهزة سريعة ومبنية على Tailwind.
- **React Router DOM (v7.13.0)**: لإنشاء المسارات (Routes) وتوجيه المستخدمين بين الصفحات.
- **Framer Motion**: لإضافة حركات وأنيميشن سلس للتطبيق.
- **Leaflet & React-Leaflet**: لعرض خريطة تفاعلية توضح مراكز التوحد.
- **Google Gemini AI**: لدمج ذكاء اصطناعي (Chatbot) يتفاعل مع الآباء والأطفال.

---

## 📁 هيكل المشروع (Project Structure)
المشروع مرتب ومنظم بشكل يسهل تطويره وصيانته، ويتركز العمل الأساسي داخل مجلد `src`:

- **`main.jsx` & `App.jsx`**: نقطة البداية للتطبيق، حيث يتم تغليف التطبيق بمزودي الحالة (Providers) مثل `HeroUIProvider` وتحديد جميع المسارات (Routes).
- **`index.css`**: ملف التنسيقات العامة والخاصة بـ Tailwind.
- **`components/`**: المكونات القابلة لإعادة الاستخدام:
  - `AutismSupportBot.jsx` (الشات بوت الذكي المدعوم بالذكاء الاصطناعي).
  - `ClinicsMap.jsx` (خريطة المراكز المدمجة).
  - `MainNavbar.jsx` (شريط التنقل الموحد).
  - `ProtectedRoute.jsx` و `GuestRoute.jsx` (لحماية مسارات المستخدمين وتوجيههم).
  - بالإضافة لمكونات خاصة بكل لوحة تحكم (لوحة ولي الأمر والطبيب).
- **`pages/`**: يحتوي على جميع صفحات التطبيق (مثل `LandingPage`, `ChoicePage`, `ChildHomePage`, `DashboardPage`, `DoctorPage` وغيرها).
- **`context/`**: إدارة الحالة العامة للتطبيق عبر Context API:
  - `AppContext.jsx`: للتحكم في المظهر (Light/Dark) واللغة (عربي/إنجليزي).
  - `AuthContext.jsx`: لإدارة تسجيل الدخول والتسجيل والمصادقة للأدوار الثلاثة.
  - `DataContext.jsx`: لجمع وتحليل الإحصائيات الخاصة بتفاعل الطفل مع التطبيق.
- **`data/`**: البيانات الثابتة (Mock Data) أو قواعد المعرفة:
  - بيانات المشاعر، الروتين، المراكز، بطاقات PECS.
  - `autismKnowledgeBase.js` (قاعدة المعرفة المحلية للتوحد).
- **`api/`**: 
  - `geminiAI.js`: ملف إعداد اتصال تطبيق الذكاء الاصطناعي (Gemini).

---

## 🔐 نظام المصادقة والتوجيه (Routing & Auth)
التطبيق مصمم بثلاثة أدوار رئيسية يتم تخزين بياناتهم وجلساتهم في الـ `localStorage`:
1. **الطفل**: تسجيل الدخول أو الاشتراك عبر (`/child-login`, `/child-signup`). عند نجاح الدخول يتم توجيهه إلى `/child-home`.
2. **ولي الأمر**: تسجيل عبر (`/login`, `/signup`). يدير حساب الطفل ويتابع الإحصائيات عبر `/parent-dashboard`.
3. **الطبيب**: تسجيل عبر (`/doctor-auth`). يدير المرضى والتقارير عبر `/doctor-dashboard`.

يتم حماية هذه الصفحات باستخدام `ProtectedRoute` للتأكد من عدم وصول الأشخاص غير المصرح لهم، واستخدام `GuestRoute` لمنع المستخدمين المسجلين من العودة لصفحات تسجيل الدخول.

---

## 🎨 واجهات الأطفال التفاعلية
التطبيق يوفر واجهات متخصصة للطفل في مسار `/child-home` وما يتفرع منه:
- **نظام PECS (`/pecs`)**: واجهة تواصل بالصور تسمح للطفل باختيار الكلمات وتركيب جمل ونطقها آليًا باستخدام `Web Speech API`.
- **المشاعر (`/emotions`)**: دروس لتعلم المشاعر بمستويات صعوبة مختلفة مع اختبارات تقييمية.
- **الروتين اليومي (`/routine`)**: جدول بصري يُقسم اليوم إلى فترات مع إمكانية تعليم الأنشطة كمكتملة.
- **الهدوء (`/calming`)**: منطقة استرخاء توفر تمارين تنفس تفاعلية ومؤقت.

---

## 📊 لوحات التحكم (Dashboards)
- **لوحة تحكم ولي الأمر (`/parent-dashboard`)**: شاشة شاملة توفر رسمًا بيانيًا للاستخدام، ومراقبة مستوى الطفل، تقارير طبية، خريطة للمراكز المتخصصة القريبة، والوصول إلى بوت الدعم الآلي.
- **لوحة تحكم الطبيب (`/doctor-dashboard`)**: تتيح إضافة الأطفال إلى قائمة المرضى، إجراء تقييمات (Assessments)، مراقبة السلوك، وصياغة تقارير طبية دقيقة تُحدث بيانات الطفل لدى ولي الأمر.

---

## 💡 التحديثات الأخيرة والمعمارية الحالية
تم مؤخرًا تنفيذ عدد من التحديثات المعمارية التي تحسن من قابلية صيانة التطبيق وتجربة المستخدم:
- دمج `MainNavbar.jsx` ليكون شريط انتقال رئيسي ذكي يخدم كافة الأدوار بطريقة متسقة.
- نقل الإعدادات مثل تبديل اللغة والوضع المظلم إلى صفحة مستقلة (`/settings`).
- تحويل واجهات الملف الشخصي (Profiles) إلى صفحات كاملة مستقلة بدلًا من النوافذ المنبثقة للـ Routing الأفضل.
- العمل بذكاء مع بيانات الـ `localStorage` لضمان عمل التطبيق بسلاسة كـ Offline-First و Local-First application.

تم قراءة وفحص هذه الملفات بنجاح، وجميع إعداداتها ومساراتها مهيئة لتعمل بسلاسة ضمن بيئة `Vite`.
